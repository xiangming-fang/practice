server:
  port: 8090
  servlet:
    application-display-name: applicationContext



# spring
spring:
  mvc:
    view:
      prefix: /static/
      suffix: .html
  jpa:
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost/xiaohaizi?characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    lettuce:
      pool:
        max-idle: 300
        max-active: 8000
        max-wait: 500

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml









